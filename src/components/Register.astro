---
import Button2 from "./partials/Button2.astro"

---

<section id="register">
    <h2><span class="icon-user-plus"></span> Inscrivez-vous à nos ateliers<span class="terminal-cursor"></span></h2>
    <div class="register-section">
        <h3>Comment participer ?</h3>
        <p>Nos ateliers sont ouverts à tous, débutants comme expérimentés. Pour participer, il suffit de vous inscrire via Discord ou directement auprès de nous par email. Il est tout à fait possible de venir à nos ateliers sans s'inscrire.</p>
        
        <Button2 label="Rejoindre notre Discord" link="https://discord.gg/2H8YcxMAWE" icon="💬 " />
        <Button2 label="Nous contacter par email" link="mailto:livecodingstrasbourg@gmail.com" icon="📧 " />
        
    </div>
</section>

<style>
.register-section {
    background-color: var(--card-background);
    padding: 2rem;
    box-shadow: var(--box-shadow);
    text-align: center;
    border: 1px solid var(--primary-dark);
    position: relative;
    overflow: hidden;
}

.register-section::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
        linear-gradient(to right, rgba(0, 255, 255, 0.05) 1px, transparent 1px),
        linear-gradient(to bottom, rgba(0, 255, 255, 0.05) 1px, transparent 1px);
    background-size: 20px 20px;
    z-index: -1;
}

.register-section h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--primary-light);
    font-family: monospace;
    text-shadow: 1px 1px 0 var(--background-dark);
}

.register-section p {
    margin-bottom: 1.5rem;
    font-family: monospace;
}
</style>